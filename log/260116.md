# task 
1. intercode
2. asmgen


现在asmgen写好了，整个的编译器算是初步的完成，接下来就是要精细化。

1. 添加临时变量的分配，完善汇编生成，先能通过nasm编译。
2. 完善parser，使之能够支持无值的return，增强健壮性。
3. 支持全局变量
全局变量有一个问题就是，全局变量的初始化。
一个解决办法是，添加一个_start函数，初始化代码放在里面。
4. 优化CODE_MOV的asm生成
现在是直接mov的，但是nasm里面全局变量就不能这样，可能需要分化出一个专门mov全局变量的intercode。
5. 分开数据段和代码段
不分开的话默认会分到text段里面。
6. 增加对函数调用的支持
目前不支持函数调用，主要是因为parser的handler和scan arglist和intercode参数传递部分还没做好，接下来要写好。
为了之后能使用库函数，需要一个extern关键字，作为一个NODE_DECLARATION，然后这个declaration可以用在没有extern的情况下，就是直接声明了。
可以做成：
extern: NODE_EXTERN_DECLARATION
普通声明:NODE_DECLARATION
7. parser代码优化
parser之后会变得更长，所以要做好代码复用。

