

# task

1. 扩展list_t
改成存储数据的，就像常规的vector一样。

2. 全局变量和字符串常量
有几个需要注意的点：
全局变量汇编生成时，需要[g]取内容
字符串常量则不需要加[]，直接存地址即可。
那就搞两个中间码

现在就可以直接编译生成的汇编代码了！

现在遇到一个问题，如果赋值语句直接右边直接是一个变量的话，生成的汇编就会直接在两个变量之间传送，nasm是不允许这样的。

3. 字符常量支持

字符常量相对比较简单吧，目前也可以不另外添加类型，直接当作int就可以，常量到时候直接翻译成对应的ascii码就好。
