因为被rust离谱的严格检查气死了，所以决定用c重新开始。
很久没有体验这种古法写代码的感觉了。

# task

lexer部分，
使用列表里面列出

{
判断允许开头函数begin，
判断允许函数allowed,
扫描完毕后函数after

}

begin用于判断是否是允许的开头，
allowed用于在中间扫描时判断是否是允许的字符，来扫描连续的字符，
after用于扫描完毕后对对字符串进行鉴别以及转化成token
2
然后判断顺序有讲究，关键字要在标识符前面。
